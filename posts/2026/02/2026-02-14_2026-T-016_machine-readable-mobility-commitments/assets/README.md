# Assets (Needed)

This post is stronger with one concrete artifact. Suggested captures (redact as needed):

- Architecture sketch: rule objects + feed ingestion + evaluation + audit log
- Example policy JSON (redacted): fields, versioning, and provenance
- Exception log mock: cannot-verify -> escalation -> resolved
